<% if !categories.empty? %>
  <div class="flex gap-4 mt-3">
    <% categories.each do |category| %>
      <div class="bg-purple-500 bg-opacity-50 border border-purple-600 text-white px-3 py-1 rounded-full text-sm">
        <%= category.name %>
      </div>
    <% end %>
  </div>
<% end %>